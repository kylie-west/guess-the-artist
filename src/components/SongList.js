import React from "react";
import Card from "./Card";

const SongList = ({ songs }) => {
	return (
		<div>
			{/* {songs.map((song, id) => (
                <Song url={song.url} />
			))} */}
		</div>
	);
};
